import SwiftUI

// Placeholder entry point — replaced by the UI workstream.
@main
struct SafeRoutesApp: App {
    var body: some Scene {
        WindowGroup {
            Text("SafeRoutes — UI under construction")
                .frame(minWidth: 480, minHeight: 320)
        }
    }
}
